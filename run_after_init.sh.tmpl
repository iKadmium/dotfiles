{{ if ne .chezmoi.os "windows" -}}
#!/bin/sh
set -e

if ! command -v nu >/dev/null 2>&1; then
    echo "Error: nushell (nu) is not installed or not in PATH" >&2
    exit 1
fi

nu "$CHEZMOI_SOURCE_DIR/init.nu"
{{ end -}}