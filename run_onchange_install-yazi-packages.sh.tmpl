{{ if ne .chezmoi.os "windows" -}}
#!/bin/bash

# package.toml hash: {{ include "dot_config/yazi/package.toml" | sha256sum }}

if command -v ya >/dev/null 2>&1; then
    echo "Detected changes in yazi/package.toml. Syncing packages..."
    ya pkg install
else
    echo "Warning: 'ya' binary not found. Skipping Yazi package sync."
fi
{{ end -}}